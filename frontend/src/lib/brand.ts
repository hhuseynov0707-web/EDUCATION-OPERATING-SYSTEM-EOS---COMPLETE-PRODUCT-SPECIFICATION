// Display name for the academy. Set NEXT_PUBLIC_APP_NAME in Vercel (or .env.local)
// to your own course/academy name — no code change needed.
export const APP_NAME = process.env.NEXT_PUBLIC_APP_NAME?.trim() || 'EOS';
